// Placeholder; real PKCE auth comes in a later step.
export function login(){ alert("Auth wiring comes in the next step ✨"); }
export function logout(){ localStorage.clear(); location.reload(); }
export function getToken(){ return null; }
export async function handleRedirect(){ return null; }
