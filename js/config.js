// Filled properly in a later step:
export const SPOTIFY_CLIENT_ID = ""; // will set later
export const REDIRECT_URI = window.location.origin + window.location.pathname; // your Pages URL
export const SCOPES = ["user-top-read"]; // can add more later

// Temporary playlist IDs (we’ll replace with yours later)
export const PLAYLIST_SIDE_A = ""; 
export const PLAYLIST_SIDE_B = "";
